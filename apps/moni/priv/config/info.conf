{now,""}.
{n_procs, ""}.
{wall_clock, "wall_clock can be used in the same manner as runtime, except that real time is measured as opposed to runtime or CPU time."}.
{runtime,"is the sum of the run-time for all threads in the Erlang run-time system and may therefore be greater than the wall-clock time."}.
{run_queue,"Returns the length of the run queue, that is, the number of processes that are ready to run."}.
{alloc_areas,""}.
{memi,""}.
{procinfo,""}.